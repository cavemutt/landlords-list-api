spring:
  datasource:
    username: landlord_list
    password: landlord_list
    url: jdbc:mysql://localhost:3307/landlord_list
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    
  sql:
    init:
      mode: never  